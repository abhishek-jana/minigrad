[project]
name = "minigrad"
version = "0.1.0"
description = "A minimal autograd notebook project inspired by micrograd."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "graphviz>=0.20.3",
  "ipykernel>=6.29.5",
]

[tool.uv]
package = false
